{% if attribute.value %}<span class="link"><a href="{{ attribute.value['value'] }}" title="{{ attribute.value['title'] }}" target="{{ attribute.value['target'] }}">{{ attribute.value['label'] }}</a></span>{% endif %}